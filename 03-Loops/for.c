#include <stdio.h>

int main() {
    // With loops (concise and maintainable)
    for (int i = 1; i <= 10; i++) { // starting with value 1, to maximum equals to 10, incrementing by 1
        printf("%d\n", i);
    }
    
    return 0;
}